# Tree Register 開発指針

## 1. 目的と参照元

Windows 11上のフォルダを探索し、DCATに相当するカタログ・データセットの登録設定を作成するデスクトップアプリを開発する。

- UI・操作の参照元は同じディレクトリの `index.html`。
- この文書はHTMLの現行機能と指定技術スタックを実装要件として整理したもの。
- HTMLは操作確認用の試作であり、単一ファイル構成や直接DOM操作を本実装へ持ち込まない。
- 最新のユーザー指示を優先する。特に階層上限超過は「超過項目だけを除外」ではなく「読込全体を停止」とする。
- 日本語UIを基本とする。

## 2. 必須技術スタック

| 項目 | 採用技術・制約 |
| --- | --- |
| 対象OS | Windows 11のみ |
| デスクトップアプリ | Electron |
| 画面 | React・TypeScript |
| UIコンポーネント | Material UI（`@mui/material`） |
| データ保存 | SQLite |
| SQLiteアクセス | Electron同梱Node.jsの `node:sqlite` |
| パッケージ管理 | npm。`package-lock.json` を管理する |
| ビルド | ビルドツールが必要な場合はVite |

- 別のSQLiteドライバや外部Node.jsランタイムへの依存に置き換えない。
- Electronの採用バージョンを決める際に、同梱Node.jsで必要な `node:sqlite` APIが利用できることを確認する。
- 開発PCのNode.jsで動くことだけで互換性を判断しない。Electron実行環境とパッケージ後のアプリで確認する。
- 依存バージョンを推測で固定せず、実装開始時に互換性を確認する。
- Windows以外への対応は現在のスコープに含めない。

## 3. 用語とDCAT構造

「カタログ」は常にDCATのデータカタログに相当する構造として扱う。

| UI上の概念 | 対応する構造 |
| --- | --- |
| カタログ | `dcat:Catalog` |
| サブカタログ | 親カタログから `dcat:catalog` で関連付けるカタログ |
| データセット | `dcat:Dataset`。親カタログから `dcat:dataset` で関連付ける |
| 元ファイル | データセットの配布である `dcat:Distribution` に対応 |

- 元のフォルダ階層と登録後のカタログ階層を別に保持する。
- ファイルを単独カタログにした場合、独立したカタログと、その中のデータセット1件を作る。元ファイルはそのデータセットの配布とする。
- 自動生成したデータセットには単独カタログのタイトル・説明・タグを反映する。
- JSON出力はアプリの登録設定であり、DCATのRDF出力と混同しない。

## 4. 画面構成

HTMLの情報構成を維持し、Material UIで再構成する。

1. ヘッダー：アプリ名、フォルダ選択、サンプル再読込。
2. 共通設定：最大階層、管理元ID、登録済み台帳の読込。
3. 状態通知：探索状況、階層超過エラー、登録済み階層の検出結果。
4. 左ペイン：名前検索、フォルダツリー、登録区分と登録済みの表示、操作ガイド。
5. 中央ペイン：選択パス・階層、登録区分、タイトル・説明、継承設定、タグ選択。
6. 右ペイン：カタログ・データセット・対象外の件数、登録プレビュー、検証結果、JSON保存。

- `Alert`、`TextField`、`RadioGroup`、`Chip`、`Button` 等を用途に合わせて使用する。
- 登録済みやエラーは色だけで区別せず、文言でも示す。
- 狭いウィンドウでも操作できる配置にする。
- デモ台帳・サンプル・大量データデモは実データと明確に区別する。

## 5. フォルダ選択・探索

- Electronのフォルダ選択ダイアログで対象を選ぶ。
- Node.js側でフォルダを逐次探索する。ファイル本文は読まず、必要なパス・種類等の情報を取得する。
- 空フォルダもツリーへ取り込む。HTMLのブラウザ互換用フォールバックは不要。
- 1万件以上のファイル・フォルダを想定し、探索・検証・描画で画面を固めない。
- 探索件数と現在の状態を表示する。総数が未確定なら「残り未探索」と示し、総数算出のための全件事前探索は行わない。
- 検査中のデータは一時領域へ保持し、全件の検査成功後に作業中データを置き換える。
- エラー時には一時データを破棄し、直前の設定を保持する。
- 権限不足・読込失敗・探索中の削除等も利用者に説明できるエラーとして扱い、不完全な探索結果を成功扱いしない。
- ジャンクションやシンボリックリンク等による循環・選択範囲外の探索を防ぐ。追跡方針を実装時に明示し、黙って無制限に追跡しない。

## 6. 階層上限と全体停止

### 数え方

- 選択したルートフォルダを階層1とする。
- その直下のフォルダ・ファイルは階層2とする。
- 最大階層の初期値は3。設定値は1以上の整数。
- 元フォルダ内の深さで判定する。登録区分をトップレベルにしても深さ判定は変わらない。
- 単独カタログに内包する自動生成データセットは、元ファイルと同じ深さ判定とする。

### 停止動作

- 上限を超える項目を1件検出した時点で探索全体を停止する。
- 登録対象外に指定された項目でも、探索時の深さ検査は省略しない。
- 超過した階層のさらに下へ探索を進めない。
- 正常部分だけを部分取込・部分登録しない。
- 検査中とエラー停止中は登録設定の確定・JSON保存を禁止する。
- エラーには超過パス、検出階層、設定上限、停止までの確認件数を含める。総数は既知の場合だけ表示する。
- 「上限を変更してフォルダを再選択」「前の設定に戻る」の復帰手段を提供する。
- 失敗した読込は、上限を変更しただけでは成功にならない。再探索が必要。
- 読込成功済みデータの上限を引き下げた場合も再検証し、違反があれば処理を停止する。
- 1万件を想定した階層超過デモを用意する。HTMLでは上限3の場合に751件目で超過を検出する。

## 7. 登録区分

| 対象 | 選択肢 | 初期値 |
| --- | --- | --- |
| ルートフォルダ | トップレベルカタログ／サブカタログ／登録対象外 | トップレベル |
| 子フォルダ | トップレベルカタログ／サブカタログ／登録対象外 | サブカタログ |
| ファイル | 親カタログ内のデータセット／単独カタログ／登録対象外 | データセット |

- サブカタログと通常のデータセットには有効な親カタログが必要。親のないサブカタログ設定はエラーにする。
- フォルダを対象外にした場合は配下もすべて対象外にする。
- 一時的な対象外や継承設定の切り替えで固有メタデータを失わない。
- 原要件のファイルの「登録とする」は、HTMLと同様に「登録対象外とする」と解釈している。指示で訂正された場合は追従する。

## 8. メタデータとタグ

### タイトル・説明・継承

- カタログとデータセットに固有のタイトル・説明文を設定できる。
- タイトルは空白のみを許容せず必須。説明文とタグは任意。
- 通常のデータセットは「固有登録」「親と同じ内容」を切り替えられる。
- 継承対象はタイトル・説明・タグ。親の変更を反映し、継承中は該当入力を編集不可にする。
- 保存する固有値と、継承を解決した表示・出力値を区別する。

### タグ選択・分類

- 100件を超える事前設定タグから検索・複数選択できる。
- HTMLの初期サンプルは132タグ・11カテゴリ。実データの上限としない。
- タグ名検索とカテゴリ絞り込みを組み合わせる。
- 検索は英字の大文字・小文字、全角・半角の差を吸収する。
- 検索条件を変えても選択を保持する。選択済みタグは別枠で表示し、個別解除できる。
- 候補件数・全タグ数・選択数・検索結果なしの状態を表示する。
- カテゴリ追加とタグのカテゴリ変更を可能にする。分類は全項目共通のタグマスタで管理する。
- 現行仕様では1タグにつき1カテゴリ。1項目には複数タグを設定できる。
- カテゴリ名は空白のみ・重複を禁止する。タグとカテゴリに安定したIDを付け、表示名や分類変更で選択関係が壊れないようにする。

## 9. 登録済み階層と二重登録の防止

- SQLiteに登録済み台帳を保存し、探索結果と照合する。
- 検出時は登録パス、タイトル、カタログID、登録日を表示する。
- 登録済みの階層とその配下をツリー・詳細画面で識別できるようにする。
- 一致階層だけでなく、登録済み親階層に含まれる選択対象も検出する。
- 登録済み階層が登録対象に残る間は、登録設定の確定・JSON保存を禁止する。
- 「登録済み階層をまとめて対象外にする」を提供し、配下にも反映する。
- 名前の部分一致で判定しない。例えば `folder1` と `folder10` は別階層。
- 管理元IDを必須にする。同名フォルダが異なる場所にある場合を区別する。
- HTML互換の台帳は管理元IDと選択フォルダ名を含む相対パスで照合する。Electron側では管理元IDと基準位置の対応も保存し、選択位置を変えたことで重複が見逃されないようにする。
- Windowsの区切り文字、末尾区切り、実際のパス同一性を考慮した比較キーと、表示用パスを分ける。単純な文字列前方一致だけで階層を判定しない。
- 管理元の対応関係を確定できない場合は照合済みと表示しない。

### 台帳JSONの互換形式

```json
{
  "entries": [
    {
      "sourceId": "demo",
      "path": "地域データアーカイブ/01_人口・世帯",
      "catalogId": "CAT-2026-0042",
      "title": "人口・世帯統計",
      "registeredAt": "2026-09-01"
    }
  ]
}
```

- インポート前に必須項目・型・パスを検証する。HTMLのファイルサイズ上限は5,000,000バイト。
- 不正JSONや検証失敗で既存台帳を消さない。置換・取込はトランザクションで行う。
- 設定JSONの保存を登録完了として台帳に追加しない。
- 外部カタログへの登録APIと登録完了通知は未定義。勝手に接続先や登録成功処理を実装しない。

## 10. キーボードと大量データUI

| キー | ツリーにフォーカスがある場合の操作 |
| --- | --- |
| ↑ / ↓ | 現在表示されている前／次の項目を選択 |
| → | 閉じた項目を展開。展開済みなら子項目へ移動 |
| ← | 展開項目を閉じる。閉じている場合等は親へ移動 |
| Home / End | 表示対象の先頭／末尾へ移動 |
| Enter | 選択項目の設定欄へフォーカスを移動 |
| Tab / Shift+Tab | 通常の入力・ペイン間フォーカス移動 |

- 入力欄の矢印キー操作をツリーのショートカットで奪わない。
- 検索と折りたたみを考慮した表示順で移動する。
- 検索結果0件、先頭・末尾、子のない項目でも例外を発生させない。
- 選択項目を画面内へスクロールし、フォーカスと選択状態を視覚的に区別する。
- ツリーの役割・階層・選択・展開状態をアクセシビリティ属性で提供する。
- 仮想スクロールで可視範囲と少量の予備行だけを描画し、画面外の項目もキーボードで選択できるようにする。
- 検索・折りたたみで件数が減った際はスクロール位置を補正する。
- プレビューを制限する場合は表示件数と総数を明示する。HTMLでは先頭100件表示だが、JSON保存は全件が対象。

## 11. Electronの責務分離

### Main・バックグラウンド処理

- フォルダ選択、ファイル探索、台帳照合、SQLite、JSON入出力を担当する。
- 大規模探索と同期的なSQLite処理は専用ワーカー等へ分離し、MainとRendererの応答を維持する。
- DB接続の所有者を明確にし、書込を直列化する。
- 探索ごとに処理IDを付け、以前の処理から届いた進捗が新しい状態を上書きしないようにする。

### Preload・IPC

- `contextBridge` を介して目的別の型付きAPIを公開する。
- Rendererから任意のSQL・任意のファイル操作・汎用IPC送信を実行できるAPIを公開しない。
- IPC入力と返却データを検証し、進捗通知の購読解除を実装する。
- 階層上限・重複・タイトル必須等の検証はMain側でも実施する。ボタンの無効化だけに依存しない。

### Renderer

- React・TypeScriptとMaterial UIで表示、入力、選択、検索、フォーカスを管理する。
- Node.js、ファイルシステム、`node:sqlite` に直接アクセスしない。
- `contextIsolation` を有効にし、Rendererの `nodeIntegration` を無効にする。
- `node:sqlite` はElectron側の実行時依存として扱い、Renderer用バンドルへ含めない。

## 12. SQLiteと状態管理

以下は実装時の論理モデルの目安。命名は変更できるが責務を維持する。

| データ | 内容 |
| --- | --- |
| 設定 | 最大階層、管理元と基準位置の対応等 |
| 作業領域 | 選択ルート、最終成功した探索、編集状態 |
| 元項目 | 親ID、種類、パス、深さ、登録区分、固有メタデータ、継承設定 |
| タグ・カテゴリ | マスタと分類、項目とタグの多対多関係 |
| 登録済み台帳 | 管理元、登録パス、比較キー、カタログID、タイトル、登録日 |
| スキーマ履歴 | マイグレーションバージョン |

- DBはElectronのユーザーデータ領域に保存し、アプリのインストール先や選択した元フォルダへ書き込まない。
- HTMLの「閉じると設定が消える」は本実装へ引き継がない。確定した編集・設定・タグ分類・台帳をSQLiteへ永続化する。
- 読込中の一時状態と最後に成功した作業状態を分離する。
- 探索結果の置換、台帳インポート、関連データの更新にはトランザクションを用いる。
- 外部キー、適切な一意制約、親ID・照合キーへの索引を設ける。
- SQLはパラメータ化し、文字列連結で利用者入力を埋め込まない。
- 検索・ツリー展開・継承解決ごとの全件再走査や、項目数に対して二乗で増える処理を避ける。
- アプリ再起動で編集状態が復元されても、ファイル状態や台帳との整合性を必要に応じて再検証する。

## 13. JSON出力

- HTMLの `tree-register-config`、`version: 3` を互換性の参照とする。
- 管理元、作成日時、タグ・カテゴリ、適用ルール、カタログ、データセット、対象外パス、元項目の設定を含める。
- 内部DBスキーマと外部JSON形式を分離する。形式を変更する場合はバージョンを更新する。
- 階層検査未完了・エラー停止・未解消の登録済み重複・必須入力エラー・登録対象なしでは保存不可。
- 保存ダイアログのキャンセルは正常な中断とし、ファイル書込成功後にのみ保存完了を表示する。
- 外部カタログへの送信・登録は現時点のスコープ外。

## 14. 推奨コード構成

```text
src/
  main/          # Electron起動、ダイアログ、IPC受付
  preload/       # Rendererへ公開するAPI
  workers/       # 探索・DBなどのバックグラウンド処理
  database/      # node:sqlite、マイグレーション、リポジトリ
  domain/        # 深さ・継承・登録構造・重複判定
  shared/        # IPC・JSONの型、検証用定義
  renderer/      # React・MUIの画面と状態管理
tests/
  fixtures/      # 大量データ・階層超過・台帳のテストデータ
```

- ドメイン判定をUIから分離し、画面と保存処理で同じルールを使う。
- TypeScriptの厳格な型検査を基本とする。
- npmに開発起動、型検査、テスト、ビルドのスクリプトを用意し、実際のコマンドをREADMEへ記載する。
- プロトタイプ `index.html` は参照用として保持し、変更が必要な場合は要件との差異を説明する。

## 15. 検証・完了条件

最低限、次を確認する。

1. フォルダ選択、空フォルダ、通常ファイルがWindows 11で表示される。
2. 深さが上限と同じなら成功し、1つ超えた時点で全体停止する。
3. 超過後の項目を探索せず、途中データが作業領域・DBへ確定されない。
4. 階層上限変更、再探索、前の設定への復帰が正しく動く。
5. 1万件以上の正常読込と途中停止で画面が応答し、DOMの行数が全件数に比例して増えない。
6. 親子関係、単独カタログ内のデータセット・配布、対象外の配下反映が正しい。
7. 継承の反映と固有値の保持ができ、タイトル未入力で保存できない。
8. タグ検索と分類絞り込みを切り替えても選択が残り、分類変更後も選択関係が保持される。
9. 登録済み親・子階層を検出し、似た名前の別階層を誤判定しない。
10. 不正な台帳JSONで既存データが失われず、重複対象が残ると保存できない。
11. 仮想スクロール・検索・空結果・折りたたみの状態でもキーボード操作とフォーカスが正しい。
12. JSON保存が登録済み台帳を更新せず、全対象の構造・タグ・継承結果を正しく出力する。
13. 再起動後にSQLiteの保存内容を復元でき、失敗した読込が以前の設定を破壊していない。
14. Windows 11のパッケージ済みElectronで `node:sqlite` の起動・読込・書込が成功する。

実施した検証と未実施の検証を区別して報告する。ロジックテストだけで実画面・パッケージ動作を確認済みとしない。
