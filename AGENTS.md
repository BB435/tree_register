# Tree Register 開発指針

## 優先順位と対象

- 最新のユーザー指示を最優先する。
- 対象OSはWindows 11。UIは日本語を基本とする。
- 本ファイルをプロジェクト要件・実装方針の唯一の正本とする。

## 技術制約

- Electron、React、TypeScript、Material UI、SQLiteを使用する。
- SQLiteアクセスはElectron同梱Node.jsの`node:sqlite`に限定する。
- RendererからNode.js、ファイルシステム、SQLiteへ直接アクセスさせない。
- `contextIsolation`を有効化し、Rendererの`nodeIntegration`を無効化する。
- IPCは目的別・型付きAPIのみ公開し、任意SQL・任意ファイル操作・汎用IPCを公開しない。
- ルートの参照用`index.html`は保持する。Reactのエントリは`src/renderer/index.html`。

## アプリの責務

- Main／Worker：フォルダ選択、探索、台帳照合、SQLite、JSON入出力。
- Renderer：React・MUIによる表示、入力、検索、ツリー、表形式編集、キーボード操作。
- 探索結果、編集内容、タグ・カテゴリ、登録済み台帳、共通設定はSQLiteへ保存し、再起動後に復元する。
- JSON出力は登録設定の出力であり、外部カタログへの登録完了や台帳更新を意味しない。
- 外部カタログ登録APIや登録成功処理は実装しない。

## データモデルと登録ルール

- ルートフォルダの深さを1とし、設定した最大階層で判定する。
- 階層超過時の動作は「エラーで全体停止」と「指定階層まで登録」を設定可能にする。初期値は全体停止。
- 登録区分は、フォルダがトップレベルカタログ／サブカタログ／対象外、ファイルがデータセット／単独カタログ／対象外。
- 対象外フォルダの配下は対象外にする。親カタログのないサブカタログ・データセットはエラーにする。
- タイトルは空白のみを不可とし、説明・タグ・カテゴリは任意とする。
- データセットの継承設定ではタイトル・説明・タグを親から解決し、固有値は保持する。
- カタログカテゴリとタグカテゴリを安定IDで管理する。タグは複数選択可能、タグごとのカテゴリは1つとする。
- 指定拡張子は再探索時の初期登録対象外として適用する。
- Windowsのパスは区切り、末尾、大小文字、階層境界を考慮して比較する。単純な文字列前方一致は禁止する。

## UI・アクセシビリティ

- ツリーは`↑↓`、`←→`、`Home`、`End`、`Enter`で操作できるようにする。
- 入力欄の矢印キーをツリー操作で奪わない。
- Tab順、フォーカス表示、Enter／Escape操作、空結果を考慮する。
- ツリーの役割、階層、選択、展開状態をARIA属性で提供する。
- 大量データでは仮想化または可視範囲描画を使い、全件を一度にDOMへ追加しない。
- 登録設定画面、検索画面、タグ一覧、表形式登録画面の状態と保存結果を一致させる。

## 探索・保存・検証

- 探索は逐次処理し、進捗と現在状態を表示する。ファイル本文は読まない。
- 探索中は一時状態に保持し、成功時にのみ作業領域を置き換える。失敗時は直前の成功状態を保持する。
- シンボリックリンク、ジャンクション、循環、選択範囲外への遷移を拒否する。
- SQLiteの更新はWorker側で直列化し、トランザクションとパラメータ化SQLを使う。
- 台帳JSONは入力検証後に置換し、不正入力で既存台帳を消さない。
- JSON出力前に階層超過、重複、タイトル必須、登録対象なしなどを再検証する。

## ファイルと検証

- 編集対象は`src/`と`tests/`。`dist/`・`release/`はビルド生成物として直接編集しない。
- 変更後は原則として次を実行する。

  - `npm.cmd run typecheck`
  - `npm.cmd test`
  - `npm.cmd run build`

- Electron実動作確認は`npm.cmd run test:electron`を使い、テスト専用データで利用者のDBを変更しない。
- 実施した検証と未実施の検証を最終報告で区別する。
